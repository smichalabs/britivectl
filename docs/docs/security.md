<!--
This page mirrors the project's SECURITY.md at the repo root so the docs
site has the same security story you would see on GitHub. Edit SECURITY.md
at the repo root; the pymdownx.snippets include below pulls the content
in at build time so the two stay in sync automatically.
-->

--8<-- "SECURITY.md"
